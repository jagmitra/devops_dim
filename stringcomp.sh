#!/bin/bash


if [ "learningdevops"=="learningdevops" ]
then
echo "both are similar strings"
fi

 
name1="gaurav is a good guy"
name2="bhavika is a bad girl"
if [ "${name1}"=="${name2}" ]
then
echo "string is equal"
else
echo "String is not equal"
fi
